[project]
name = "ecommerce-database"
version = "0.1.0"
description = "Async SQLAlchemy database helpers."
requires-python = ">=3.12"
dependencies = [
    "sqlalchemy>=2.0.49",
    "ecommerce-config>=0.1.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

