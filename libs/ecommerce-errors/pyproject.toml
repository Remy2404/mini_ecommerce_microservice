[project]
name = "ecommerce-errors"
version = "0.1.0"
description = "Shared application errors and FastAPI handler helpers."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.1",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

