[project]
name = "ecommerce-contracts"
version = "0.1.0"
description = "Shared API and event contracts for the mini-ecommerce services."
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.13.4",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

