[project]
name = "ecommerce-cache"
version = "0.1.0"
description = "Valkey cache client and helpers."
requires-python = ">=3.12"
dependencies = [
    "valkey>=6.1.1",
    "ecommerce-config>=0.1.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

